name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  # server/version.go is what every MCP client sees in the initialize
  # handshake, and it is set by hand via `make set-version`. Nothing is
  # published until the tag and the constant agree, so a release can never ship
  # announcing a version it is not.
  verify-version:
    name: Verify version matches tag
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4

    - name: Check server/version.go against the pushed tag
      run: |
        tag="${GITHUB_REF_NAME#v}"
        version=$(sed -n 's/^const VERSION = "\(.*\)"$/\1/p' server/version.go)
        if [ -z "$version" ]; then
          echo "::error::Could not read VERSION from server/version.go"
          exit 1
        fi
        if [ "$tag" = "$version" ]; then
          echo "server/version.go VERSION ($version) matches tag ${GITHUB_REF_NAME}"
          exit 0
        fi
        echo "::error::server/version.go VERSION is ${version} but the tag is ${GITHUB_REF_NAME}"
        echo "Run 'make set-version VERSION=${GITHUB_REF_NAME}' on master, then re-tag."
        exit 1

    - name: Set up Go
      uses: actions/setup-go@v5
      with:
        go-version-file: '.go-version'
        cache: true

    # The comparison above only proves the tag and the constant agree, so a
    # matching but malformed pair (tag v01.2.3 with VERSION "01.2.3") would pass
    # it. The shape check has to run here rather than only in the release job,
    # because build-binaries and publish-binaries do not depend on that job and
    # would otherwise publish assets regardless.
    - name: Check the version constant is well formed
      run: go test ./server/ -run 'TestVersionIsWellFormed|TestIsNumericIdentifier' -v

  release:
    name: Release
    needs: verify-version
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
      with:
        fetch-depth: 0

    - name: Set up Go
      uses: actions/setup-go@v5
      with:
        go-version-file: '.go-version'
        cache: true

    - name: Run tests
      run: go test -v ./...

    - name: Run GoReleaser
      uses: goreleaser/goreleaser-action@v5
      with:
        distribution: goreleaser
        version: latest
        args: release --clean
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        HOMEBREW_TAP_GITHUB_TOKEN: ${{ secrets.HOMEBREW_TAP_GITHUB_TOKEN }}

  build-binaries:
    name: Build Binaries
    needs: verify-version
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        include:
          - os: ubuntu-latest
            goos: linux
            goarch: amd64
          - os: ubuntu-latest
            goos: linux
            goarch: arm64
          - os: macos-latest
            goos: darwin
            goarch: amd64
          - os: macos-latest
            goos: darwin
            goarch: arm64
          - os: windows-latest
            goos: windows
            goarch: amd64
    steps:
    - uses: actions/checkout@v4

    - name: Set up Go
      uses: actions/setup-go@v5
      with:
        go-version-file: '.go-version'
        cache: true

    - name: Build binary
      env:
        GOOS: ${{ matrix.goos }}
        GOARCH: ${{ matrix.goarch }}
      shell: bash
      run: |
        output_name='slack-bridge-mcp-server-${{ matrix.goos }}-${{ matrix.goarch }}'
        if [ "${{ matrix.goos }}" = "windows" ]; then
          output_name="${output_name}.exe"
        fi
        go build -ldflags="-s -w" -o "${output_name}" .

    - name: Upload binary
      uses: actions/upload-artifact@v4
      with:
        name: binary-${{ matrix.goos }}-${{ matrix.goarch }}
        path: slack-bridge-mcp-server-*

  publish-binaries:
    name: Publish Binaries
    needs: build-binaries
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4

    - name: Download binaries
      uses: actions/download-artifact@v4
      with:
        pattern: binary-*
        merge-multiple: true

    - name: Create Release
      uses: softprops/action-gh-release@v1
      with:
        files: slack-bridge-mcp-server-*
        generate_release_notes: true
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}